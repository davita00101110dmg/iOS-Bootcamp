public struct FuelExpenses {
    public let capacity: Int
    public let consumption: Int
    
    public init(capacity: Int, consumption: Int) {
        self.capacity = capacity
        self.consumption = consumption
    }
}
